/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['mrjtrade.uz', '213.230.91.55', 'mrjtrade.result-me.uz'], // Добавляем новый домен для изображений
  },
};

export default nextConfig;